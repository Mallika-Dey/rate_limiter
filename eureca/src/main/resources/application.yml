server:
  port: 8761

spring:
  application:
    name: eureca

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: false
    fetch-registry: true
